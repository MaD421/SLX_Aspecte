# Database Details (H2)
spring.h2.console.enabled=true
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.platform=h2
spring.datasource.url=jdbc:h2:mem:slx;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE

# Database Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Thymeleaf Settings
spring.thymeleaf.cache=false

# SMTP Server Settings
mail.smtp.host=smtp.gmail.com
mail.smtp.socketFactory.port=465
mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
mail.smtp.auth=true
mail.smtp.port=465
mail.smtp.auth.username=schoolingthenet
mail.smtp.auth.password=as5235$#%RETR
mail.smtp.from=no-reply@slx.com

# Bookmark Task Settings
bookmark.task.notification.enabled=false
bookmark.task.notification.frequency=7

# Error settings
server.error.path = "/error"

# Image settings
img.listing.maxSize = 3
img.listing.maxNumber = 5